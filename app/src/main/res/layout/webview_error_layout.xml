<RelativeLayout android:id="@+id/error_layout"
    android:layout_width="match_parent"
    android:background="@color/white"
    android:layout_height="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <ImageView
        android:id="@+id/error_img"
        android:layout_width="200dp"
        android:layout_height="130dp"
        android:layout_marginTop="120dp"
        android:layout_centerHorizontal="true"
        android:src="@drawable/webview_error_img"/>

    <TextView
        android:id="@+id/error_des"
        android:layout_below="@+id/error_img"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#999999"
        android:layout_centerHorizontal="true"
        android:textSize="14dp"
        android:text="@string/webview_error_des"
        android:layout_marginTop="10dp"
        />
    <TextView
        android:id="@+id/retry_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/error_des"
        android:layout_marginTop="25dp"
        android:gravity="center"
        android:textColor="@color/color_main"
        android:layout_centerHorizontal="true"
        android:textSize="14dp"
        style="?attr/buttonBarButtonStyle"
        android:text="刷新"/>
</RelativeLayout>
